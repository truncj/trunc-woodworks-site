## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## Deploy to GitHub Pages

1. Build the project:
   `npm run build`
2. Deploy to GitHub Pages:
   `npm run deploy`
